<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SpeechMate" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:textColorPrimary">@android:color/white</item> <!-- 设置主文本颜色为白色 -->
        <item name="android:toolbarStyle">@style/MyToolbar</item> <!-- 定义 Toolbar 样式 -->
    </style>


    <style name="Theme.SpeechMate" parent="Base.Theme.SpeechMate" >
<!--        <item name="android:textColorPrimary">@android:color/white</item> &lt;!&ndash; 设置主文本颜色为白色 &ndash;&gt;-->
<!--        <item name="android:toolbarStyle">@style/MyToolbar</item> &lt;!&ndash; 定义 Toolbar 样式 &ndash;&gt;-->

    </style>
    <!-- Toolbar样式 -->
    <style name="MyToolbar" parent="Widget.AppCompat.Toolbar">
        <item name="android:titleTextColor">@android:color/white</item> <!-- 设置标题颜色为白色 -->
    </style>
</resources>